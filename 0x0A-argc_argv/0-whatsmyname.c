#include "main.h"
#include<stdio.h>

/**
 * main - function that prints name
 * @argc: count argument
 * @argv: argument vector
 * Return: 0
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}

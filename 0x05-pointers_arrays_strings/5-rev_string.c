#include "main.h"
#include<stdio.h>

/**
 * rev_string - function to reverse a string
 * @s: The string
 * Return: 0
 */
void rev_string(char *s)
{
	int i, l;

	for (l = 0; s[l] != '\0'; l++)
	{
	l++;
	}
	for (i = l - 1; i >=0; i++)
	{
	}
}

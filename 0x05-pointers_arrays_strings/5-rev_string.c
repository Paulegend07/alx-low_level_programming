#include "main.h"
#include<stdio.h>

/**
 * rev_string - function to reverse a string
 * @s: The string
 * Return: 0
 */
void rev_string(char *s)
{
	int len = 0;
	int i;

	while  (s[len] != '\0')
	{
	len++;
	}
	for (i = len - 1; i >= 0; i--)
	{
	i--;
	}
}

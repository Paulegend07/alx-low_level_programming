#include<stdio.h>

/**
 * main - A program that that prints the alphabet in lowercase
 * Return: 0
 */
int main(void)
{
	char lc;

	for
	(lc = 'a'; lc <= 'z'; lc++)
	{
	putchar(lc);
	}
	putchar('\n');
	return (0);
	}

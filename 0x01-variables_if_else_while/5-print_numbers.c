#include<stdio.h>

/**
 * main - program that prints all single digit numbers of base 10
 * Return: 0
 */
int main(void)
{
	int num;

	for (num = '0'; num <= '9'; num++)
	printf("%d\n", num);
	return (0);
	}
